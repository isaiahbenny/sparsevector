# Sparse Vector class

An S4 class that represents sparse numeric vectors, or vectors which
contain mostly zeros

## Slots

- `value`:

  a numeric vector containing the non-zero elements of the vector

- `pos`:

  an integer vector with the positions of the non-zero elements

- `length`:

  a single integer that indicates the full length of the vector
